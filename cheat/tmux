# Session management
# ==================
Ctrl-a d            # Detach from tmux

# Start a shared session:
tmux -S /tmp/your_shared_session
chmod 777 /tmp/your_shared_session

# Detach an already attached session (great if you are moving devices with different screen resolutions)
tmux attach -d 

# Window management
# =================
Ctrl-a c            # Create window
Ctrl-a n            # Next window
Ctrl-a p            # Previous window
Ctrl-a [0-9]        # Go to  window
Ctrl-a Ctrl-a       # Last window

# Pane management
# =================
Ctrl-a |            # Split pane horizontally
Ctrl-a -            # Split pane vertically
Ctrl-a Arrows       # Set active pane
Ctrl-a !            # Break pane out into new window
Ctrl-a z            # Zoom pane (like maximize/min window)
Ctrl-a x            # Destroy pane

