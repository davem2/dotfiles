# ~/.tmuxinator/pp.yml

name: some_project
root: /mnt/marge/e/dp/PP
windows:
  - pp:
      root: /mnt/marge/e/dp/PP
      layout: tiled
      panes:
        - harpers-boys:
          - cd /mnt/marge/e/dp/PP/harpers-outdoor-book-for-boys
          - git shortlog
        - impending-crisis:
          - cd /mnt/marge/e/dp/PP/the-impending-crisis
          - git shortlog
        - saturday-magazine:
          - cd /mnt/marge/e/dp/PP/the-saturday-magazine-66
          - git shortlog
        - passamaquoddy:
          - cd /mnt/marge/e/dp/PP/passamaquoddy-texts
          - git shortlog
  - ppimg: ~/pp/tools/repo/ppimg
  - dp2ppgen: ~/pp/tools/repo/dp2ppgen
  - ppgen: ~/pp/tools/repo/ppgen


# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre: sudo /etc/rc.d/mysqld start

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
# pre_window: rbenv shell 2.0.0-p247

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: byobu

# Specifies (by name or index) which window will be selected on project startup. If not set, the first window is used.
# startup_window: logs

#windows:
#  - editor:
#      layout: main-vertical
#      panes:
#        - vim
#        - guard
#  - server: bundle exec rails s
#  - logs: tail -f log/development.log
