#!/bin/sh

filename=${1%.*}
extension=${1##*.}
maxw=240
maxh=480

echo "Creating Thumbnail: $1 --> $filename-tn.$extension"

# Scale down large image to thumbnail size
convert "$1" -resize "$maxw"x"$maxh"\> "$filename-tn.$extension"
